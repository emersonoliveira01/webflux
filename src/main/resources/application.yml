spring:
  data:
    mongodb:
      uri: mongodb+srv://${DB_USER:admin}:${DB_PASSWORD}@webfluxcluster.euv2fiw.mongodb.net/${DB_NAME:webflux}?retryWrites=true&w=majority
      auto-index-creation: true
  application:
    name: webflux
